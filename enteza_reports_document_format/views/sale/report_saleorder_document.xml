<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="report_sale_order_grouped_for_categories" model="ir.actions.report">
        <field name="name">Pedido/Presupuesto Agrupado</field>
        <field name="model">sale.order</field>
        <field name="report_type">qweb-pdf</field>
        <field name="report_name">enteza_reports_document_format.format_report_sale_order_grouped_for_categories</field>
        <field name="report_file">enteza_reports_document_format.format_report_sale_order_grouped_for_categories</field>
        <field name="print_report_name">'%s' % (object._get_report_base_filename())</field>
        <field name="binding_model_id" ref="sale.model_sale_order"/>
        <field name="binding_type">report</field>
    </record>

    <template id="format_report_sale_order_grouped_for_categories">
        <t t-call="web.html_container">
            <t t-foreach="docs" t-as="doc">
                <t t-call="enteza_reports_document_format.report_saleorder_grouped_for_categories"/>
            </t>
        </t>
    </template>

    <template id="report_saleorder_grouped_for_categories">
        <t t-call="web.external_layout">
            <t t-set="doc" t-value="doc.with_context(lang=doc.partner_id.lang)"/>
            <t t-set="forced_vat" t-value="doc.fiscal_position_id.foreign_vat"/>
            <t t-set="address">
                <div t-field="doc.partner_id"
                     t-options='{"widget": "contact", "fields": ["address", "name"], "no_marker": True}'/>
                <p t-if="doc.partner_id.vat">
                    <t t-out="doc.company_id.account_fiscal_country_id.vat_label or 'Tax ID'"/>:
                    <span t-field="doc.partner_id.vat"/>
                </p>
            </t>
            <t t-if="doc.partner_shipping_id == doc.partner_invoice_id and doc.partner_invoice_id != doc.partner_id or doc.partner_shipping_id != doc.partner_invoice_id">
                <t t-set="information_block">
                    <strong t-if="doc.partner_shipping_id == doc.partner_invoice_id">
                        Dirección de envío y facturación:
                    </strong>
                    <strong t-if="doc.partner_shipping_id != doc.partner_invoice_id">
                        Dirección de facturación:
                    </strong>
                    <div t-field="doc.partner_invoice_id"
                         t-options='{"widget": "contact", "fields": ["address", "name", "phone"], "no_marker": True, "phone_icons": True}'/>
                    <t t-if="doc.partner_shipping_id != doc.partner_invoice_id">
                        <strong>Dirección de envío:</strong>
                        <div t-field="doc.partner_shipping_id"
                             t-options='{"widget": "contact", "fields": ["address", "name", "phone"], "no_marker": True, "phone_icons": True}'/>
                    </t>
                </t>
            </t>
            <div class="page">
                <div class="oe_structure"/>

                <h2 class="mt16">
                    <t t-if="not (env.context.get('proforma', False) or is_pro_forma)">
                        <span t-if="doc.state not in ['draft','sent']">Pedido #</span>
                        <span t-if="doc.state in ['draft','sent'] and not not_reserved">Reserva #</span>
                        <span t-if="doc.state in ['draft','sent'] and not_reserved">Presupuesto #</span>
                    </t>
                    <t t-if="env.context.get('proforma', False) or is_pro_forma">
                        <span>Factura pro-forma #</span>
                    </t>
                    <span t-field="doc.name"/>
                </h2>

                <div class="row mt32 mb32" id="informations">
                    <div t-if="doc.event_date" class="col-auto col-3 mw-100 mb-2">
                        <strong>Fecha Evento:</strong>
                        <p class="m-0" t-field="doc.event_date"/>
                    </div>
                    <div t-if="doc.date_order and doc.state not in ['draft','sent']" class="col-auto col-3 mw-100 mb-2">
                        <strong>Fecha de pedido:</strong>
                        <p class="m-0" t-field="doc.date_order"/>
                    </div>
                    <div t-if="doc.date_order and doc.state in ['draft','sent']" class="col-auto col-3 mw-100 mb-2">
                        <strong>Fecha de presupuesto:</strong>
                        <p class="m-0" t-field="doc.date_order" t-options='{"widget": "date"}'/>
                    </div>
                    <div t-if="doc.validity_date and doc.state in ['draft', 'sent']" class="col-auto col-3 mw-100 mb-2" name="expiration_date">
                        <strong>Expiración:</strong>
                        <p class="m-0" t-field="doc.validity_date"/>
                    </div>
                    <div t-if="doc.user_id.name" class="col-auto col-3 mw-100 mb-2">
                        <strong>Comercial:</strong>
                        <p class="m-0" t-field="doc.user_id"/>
                    </div>
                </div>

                <t t-set="display_discount" t-value="any(l.discount for l in doc.order_line)"/>

                <table class="table table-sm o_main_table">
                    <thead style="display: table-row-group">
                        <tr>
                            <th name="th_description" class="text-left">Descripción</th>
                            <th name="th_cost" class="text-right">P.Coste</th>
                            <th name="th_quantity" class="text-right">Cantidad</th>
                            <th name="th_priceunit" class="text-right">Precio</th>
                            <th name="th_discount" t-if="display_discount" class="text-right" groups="product.group_discount_per_so_line">
                                <span>Desc.%</span>
                            </th>
                            <th name="th_taxes" class="text-right">Impuestos</th>
                            <th name="th_subtotal" class="text-right">
                                <span groups="account.group_show_line_subtotals_tax_excluded">Importe</span>
                                <span groups="account.group_show_line_subtotals_tax_included">Total</span>
                            </th>
                        </tr>
                    </thead>
                    <tbody class="sale_tbody">
                        <t t-set="current_subtotal" t-value="0"/>
                        <t t-foreach="doc.used_categories" t-as="category">
                            <t t-set="product_grouped" t-value="doc.order_line.filtered(lambda l: l.product_id and l.product_id.categ_id and l.product_id.categ_id.id == category.id)"/>
                            <t t-if="product_grouped">
                                <tr>
                                    <td t-attf-colspan="#{7 if display_discount else 6}" style="text-align:left;border-bottom:2px solid black;border-top:2px solid black;background:#e1e5eb">
                                        <strong><span t-field="category.name"/></strong>
                                    </td>
                                </tr>
                            </t>
                            <t t-foreach="product_grouped" t-as="line">
                                <t t-set="current_subtotal" t-value="current_subtotal + line.price_subtotal" groups="account.group_show_line_subtotals_tax_excluded"/>
                                <t t-set="current_subtotal" t-value="current_subtotal + line.price_total" groups="account.group_show_line_subtotals_tax_included"/>

                                <tr t-att-class="'bg-200 fw-bold o_line_section' if line.display_type == 'line_section' else 'fst-italic o_line_note' if line.display_type == 'line_note' else ''">
                                    <t t-if="not line.display_type">
                                        <td name="td_name">
                                            <span t-field="line.name"/>
                                        </td>
                                        <td name="td_coste" class="text-right">
                                            <span t-if="line.product_id.rented_product_id" t-field="line.product_id.rented_product_id.list_price"/>
                                        </td>
                                        <td name="td_quantity" class="text-right">
                                            <span t-field="line.product_uom_qty"/>
                                        </td>
                                        <td name="td_priceunit" class="text-right">
                                            <span t-field="line.price_unit"/>
                                        </td>
                                        <td t-if="display_discount" class="text-right" groups="product.group_discount_per_so_line">
                                            <span t-field="line.discount"/>
                                        </td>
                                        <td name="td_taxes" class="text-right">
                                            <span t-out="', '.join(map(lambda x: (x.description or x.name), line.tax_id))"/>
                                        </td>
                                        <td name="td_subtotal" class="text-right o_price_total">
                                            <span t-field="line.price_subtotal" groups="account.group_show_line_subtotals_tax_excluded"/>
                                            <span t-field="line.price_total" groups="account.group_show_line_subtotals_tax_included"/>
                                        </td>
                                    </t>
                                    <t t-elif="line.display_type == 'line_section'">
                                        <td name="td_section_line" t-attf-colspan="#{7 if display_discount else 6}">
                                            <span t-field="line.name"/>
                                        </td>
                                        <t t-set="current_section" t-value="line"/>
                                        <t t-set="current_subtotal" t-value="0"/>
                                    </t>
                                    <t t-elif="line.display_type == 'line_note'">
                                        <td name="td_note_line" t-attf-colspan="#{7 if display_discount else 6}">
                                            <span t-field="line.name"/>
                                        </td>
                                    </t>
                                </tr>
                            </t>
                        </t>
                    </tbody>
                </table>

                <div class="clearfix" name="so_total_summary">
                    <div id="total" class="row" name="total">
                        <div t-attf-class="#{'col-6' if report_type != 'html' else 'col-sm-7 col-md-6'} ms-auto">
                            <table class="table table-sm">
                                <t t-set="tax_totals" t-value="doc.tax_totals"/>
                                <t t-set="currency" t-value="doc.currency_id"/>
                                <t t-call="account.document_tax_totals"/>
                            </table>
                        </div>
                    </div>
                </div>

                <div t-if="doc.signature" class="mt32 ms-64 me-4" name="signature">
                    <div class="offset-8">
                        <strong>Firma</strong>
                    </div>
                    <div class="offset-8">
                        <img t-att-src="image_data_uri(doc.signature)" style="max-height: 4cm; max-width: 8cm;"/>
                    </div>
                    <div class="offset-8 text-center">
                        <p t-field="doc.signed_by"/>
                    </div>
                </div>

                <div class="oe_structure"/>

                <p t-field="doc.note"/>
                <p t-if="doc.payment_term_id and doc.payment_term_id.note">
                    <span t-field="doc.payment_term_id.note"/>
                </p>
                <p id="fiscal_position_remark" t-if="doc.fiscal_position_id and doc.fiscal_position_id.sudo().note">
                    <strong>Posición fiscal:</strong>
                    <span t-field="doc.fiscal_position_id.sudo().note"/>
                </p>
            </div>
        </t>
    </template>
</odoo>
